#include "simple_netlist.h"

int parse_verilog(const char *file_name, simple_netlist &n_l, const char *key_file, const char *top_mod)
{
    std::cout << "Verilog netlists not supported yet" << std::endl; 
    return 0;
}